# NOTE: In production, use an external secret manager (GCP Secret Manager,
# Vault, or sealed-secrets). This file is a template — do NOT commit
# actual secrets to version control.
apiVersion: v1
kind: Secret
metadata:
  name: securescanner-secrets
  namespace: securescanner
type: Opaque
stringData:
  database-url: postgresql://admin:CHANGE_ME@postgres:5432/securescanner
  db-user: admin
  db-password: CHANGE_ME
  jwt-secret: CHANGE_ME_TO_RANDOM_SECRET
